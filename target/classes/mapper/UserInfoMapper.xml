<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tao.mapper.UserInfoDao">
    <select id="findUserInfoById"  resultType="UserInfo">
        SELECT * from userInfo where id=#{id}
    </select>

    <select id="findAllUserInfo" resultType="UserInfo">
        SELECT * from userInfo
    </select>

    <insert id="insertUserInfo"  parameterType="UserInfo">
        INSERT  INTO userInfo(userName,pwd,address,phone,regTime) VALUES (#{userName},#{pwd},#{address},#{phone},#{regTime})
    </insert>
</mapper>